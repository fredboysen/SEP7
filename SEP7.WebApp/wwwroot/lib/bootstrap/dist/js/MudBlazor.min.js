window.handlePieClick = (chart, elements) => {
    if (elements.length > 0) {
        const index = elements[0].index;

        // Call the Blazor method to handle the click event
        DotNet.invokeMethodAsync("SEP7.WebApp", "HandlePieClick", index);
    }
};